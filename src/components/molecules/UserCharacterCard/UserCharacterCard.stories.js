import UserCharacterCard from './UserCharacterCard';

const metaConfig = {
  title: 'components/UserCharacterCard',
  component: UserCharacterCard,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'UserCharacterCard';
