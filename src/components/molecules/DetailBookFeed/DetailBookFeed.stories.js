import DetailBookFeed from './DetailBookFeed';

const metaConfig = {
  title: 'components/DetailBookFeed',
  component: DetailBookFeed,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'DetailBookFeed';