import SearchBar from './SearchBar';

const metaConfig = {
  title: 'components/SearchBar',
  component: SearchBar,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'SearchBar';
