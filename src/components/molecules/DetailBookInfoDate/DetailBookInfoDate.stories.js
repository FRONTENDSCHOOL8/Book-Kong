import DetailBookInfoDate from './DetailBookInfoDate';

const metaConfig = {
  title: 'components/DetailBookInfoDate',
  component: DetailBookInfoDate,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'DetailBookInfoDate';
