import Card from './Card';

const metaConfig = {
  title: 'components/Card',
  component: Card,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'Card';
