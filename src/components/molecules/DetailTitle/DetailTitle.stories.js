import DetailTitle from './DetailTitle';

const metaConfig = {
  title: 'components/DetailTitle',
  component: DetailTitle,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'DetailTitle';
