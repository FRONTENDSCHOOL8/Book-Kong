import ReviewContainer from './ReviewContainer';

const metaConfig = {
  title: 'components/ReviewContainer',
  component: ReviewContainer,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};

Base.storyName = 'ReviewContainer';
