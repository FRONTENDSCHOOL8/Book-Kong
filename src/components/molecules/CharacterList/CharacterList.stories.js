import CharacterList from './CharacterList';

const metaConfig = {
  title: 'components/CharacterList',
  component: CharacterList,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'CharacterList';
