import BookCard from './BookCard';

const metaConfig = {
  title: 'components/Card',
  component: BookCard,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookCard';
