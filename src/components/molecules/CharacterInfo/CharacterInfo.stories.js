import CharacterInfo from './CharacterInfo';

const metaConfig = {
  title: 'components/CharacterInfo',
  component: CharacterInfo,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'CharacterInfo';
