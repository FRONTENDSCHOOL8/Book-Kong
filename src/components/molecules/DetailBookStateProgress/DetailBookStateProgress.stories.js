import DetailBookStateProgress from './DetailBookStateProgress';

const metaConfig = {
  title: 'components/DetailBookStateProgress',
  component: DetailBookStateProgress,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'DetailBookStateProgress';
