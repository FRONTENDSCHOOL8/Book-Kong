import ReadingStates from './ReadingStates';

const metaConfig = {
  title: 'components/ReadingStates',
  component: ReadingStates,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};

Base.storyName = 'ReadingStates';
