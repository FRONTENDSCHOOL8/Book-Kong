import RecordTitle from './RecordTitle';

const metaConfig = {
  title: 'components/RecordTitle',
  component: RecordTitle,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'RecordTitle';
