import BookInfo from './BookInfo';

const metaConfig = {
  title: 'components/BookInfo',
  component: BookInfo,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookInfo';
