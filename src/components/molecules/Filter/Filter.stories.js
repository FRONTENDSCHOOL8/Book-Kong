import Filter from './Filter';

const metaConfig = {
  title: 'components/Filter',
  component: Filter,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'Filter';
