import NoneCardState from './NoneCardState';

const metaConfig = {
  title: 'components/NoneCardList',
  component: NoneCardState,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'NoneCardState';
