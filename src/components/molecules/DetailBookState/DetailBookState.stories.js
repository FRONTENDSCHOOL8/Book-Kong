import DetailBookState from './DetailBookState';

const metaConfig = {
  title: 'components/DetailBookState',
  component: DetailBookState,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'DetailBookState';
