import DetailBookInfoPage from './DetailBookInfoPage';

const metaConfig = {
  title: 'components/DetailBookInfoPage',
  component: DetailBookInfoPage,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'DetailBookInfoPage';
