import BookDetailNavigation from './BookDetailNavigation';

const metaConfig = {
  title: 'components/BookDetailNavigation',
  component: BookDetailNavigation,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookDetailNavigation';