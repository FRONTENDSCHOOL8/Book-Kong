import BookBlock from './BookBlock';

const metaConfig = {
  title: 'components/BookBlock',
  component: BookBlock,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookBlock';
