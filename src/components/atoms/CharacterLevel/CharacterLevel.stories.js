import CharacterLevel from './CharacterLevel';

const metaConfig = {
  title: 'components/CharacterLevel',
  component: CharacterLevel,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'CharacterLevel';
