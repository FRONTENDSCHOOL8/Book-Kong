import AddBookIconButton from './AddBookIconButton';

const metaConfig = {
  title: 'components/AddBookIconButton',
  component: AddBookIconButton,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'AddBookIcon';
