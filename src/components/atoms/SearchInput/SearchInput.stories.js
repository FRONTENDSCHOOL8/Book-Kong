import SearchInput from './SearchInput';

const metaConfig = {
  title: 'components/SearchInput',
  component: SearchInput,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'SearchInput';
