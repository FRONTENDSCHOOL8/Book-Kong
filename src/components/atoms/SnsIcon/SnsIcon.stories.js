import SnsIcon from './SnsIcon';

const metaConfig = {
  title: 'components/SnsIcon',
  component: SnsIcon,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'SnsIcon';
