import DetailBookName from './DetailBookName';

const metaConfig = {
  title: 'components/DetailBookName',
  component: DetailBookName,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'DetailBookName';
