import SearchInfo from './SearchInfo';

const metaConfig = {
  title: 'components/SearchInfo',
  component: SearchInfo,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'SearchInfo';
