import DetailBookStateTitle from './DetailBookStateTitle';

const metaConfig = {
  title: 'components/DetailBookStateTitle',
  component: DetailBookStateTitle,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = '';
