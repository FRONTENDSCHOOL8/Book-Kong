import SearchIcon from './SearchIcon';

const metaConfig = {
  title: 'components/SearchIcon',
  component: SearchIcon,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'SearchIcon';
