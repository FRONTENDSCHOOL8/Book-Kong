import BookTitle from './BookTitle';

const metaConfig = {
  title: 'components/BookTitle',
  component: BookTitle,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookTitle';
