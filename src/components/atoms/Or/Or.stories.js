import Or from './Or';

const metaConfig = {
  title: 'components/Or',
  component: Or,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'Or';
