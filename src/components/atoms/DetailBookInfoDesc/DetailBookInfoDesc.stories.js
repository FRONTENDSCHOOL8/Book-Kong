import DetailBookInfoDesc from './DetailBookInfoDesc';

const metaConfig = {
  title: 'components/DetailBookInfoDesc',
  component: DetailBookInfoDesc,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'DetailBookInfoDesc';
