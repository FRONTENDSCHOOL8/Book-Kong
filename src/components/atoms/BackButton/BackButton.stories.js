import BackButton from './BackButton';

const metaConfig = {
  title: 'components/BackButton',
  component: BackButton,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = '뒤로가기 버튼';
