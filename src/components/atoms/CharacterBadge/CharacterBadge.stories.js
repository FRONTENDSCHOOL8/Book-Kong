import Badge from './CharacterBadge.stories';

const metaConfig = {
  title: 'components/Badge',
  component: Badge,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'CharacterBadge';
