import CharacterImg from './CharacterImg';

const metaConfig = {
  title: 'components/CharacterImg',
  component: CharacterImg,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'CharacterImg';
