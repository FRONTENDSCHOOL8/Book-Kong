import CharacterHeight from './CharacterHeight';

const metaConfig = {
  title: 'components/CharacterHeight',
  component: CharacterHeight,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'CharacterHeight';
