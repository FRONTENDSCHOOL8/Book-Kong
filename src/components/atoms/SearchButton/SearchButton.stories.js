import SearchButton from './SearchButton';

const metaConfig = {
  title: 'components/SearchButton',
  component: SearchButton,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'SearchButton';
