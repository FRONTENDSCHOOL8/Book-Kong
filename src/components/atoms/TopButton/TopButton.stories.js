import TopButton from './TopButton';

const metaConfig = {
  title: 'components/TopButton',
  component: TopButton,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = '';
