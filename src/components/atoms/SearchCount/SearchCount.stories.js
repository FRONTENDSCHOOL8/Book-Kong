import SearchCount from './SearchCount';

const metaConfig = {
  title: 'components/SearchCount',
  component: SearchCount,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'SearchCount';
