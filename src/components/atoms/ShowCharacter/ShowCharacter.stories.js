import ShowCharacter from './ShowCharacter';

const metaConfig = {
  title: 'components/ShowCharacter',
  component: ShowCharacter,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'ShowCharacter';
