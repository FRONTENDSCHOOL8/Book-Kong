import MemoDate from './MemoDate';

const metaConfig = {
  title: 'components/MemoDate',
  component: MemoDate,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'MemoDate';
