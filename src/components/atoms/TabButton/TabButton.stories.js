import TabButton from './TabButton';

const metaConfig = {
  title: 'components/TabButton',
  component: TabButton,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'TabButton';
