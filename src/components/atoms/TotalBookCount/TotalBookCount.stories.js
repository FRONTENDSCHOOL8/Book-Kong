import TotalBookCount from './TotalBookCount';

const metaConfig = {
  title: 'components/TotalBookCount',
  component: TotalBookCount,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'TotalBookCount';
