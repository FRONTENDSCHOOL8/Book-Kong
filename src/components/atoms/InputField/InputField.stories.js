import InputField from './InputField';

const metaConfig = {
  title: 'components/InputField',
  component: InputField,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'InputField';
