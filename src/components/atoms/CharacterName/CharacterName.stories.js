import CharacterName from './CharacterName';

const metaConfig = {
  title: 'components/CharacterName',
  component: CharacterName,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'CharacterName';
