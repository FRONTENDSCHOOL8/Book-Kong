import FilterIcon from './FilterIcon';

const metaConfig = {
  title: 'components/FilterIcon',
  component: FilterIcon,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'FilterIcon';
