import CharacterItem from './CharacterItem';

const metaConfig = {
  title: 'components/CharacterItem',
  component: CharacterItem,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'CharacterItem';
