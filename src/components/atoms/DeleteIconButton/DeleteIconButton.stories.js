import DeleteIconButton from './DeleteIconButton';

const metaConfig = {
  title: 'components/DeleteIconButton',
  component: DeleteIconButton,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = '삭제버튼';
