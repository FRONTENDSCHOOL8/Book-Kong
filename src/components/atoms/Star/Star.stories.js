import Star from './Star';

const metaConfig = {
  title: 'components/Star',
  component: Star,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'Star';
