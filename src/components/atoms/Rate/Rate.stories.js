import Rate from './Rate';

const metaConfig = {
  title: 'components/Rate',
  component: Rate,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'Rate';
