import CharacterDesc from './CharacterDesc';

const metaConfig = {
  title: 'components/CharacterDesc',
  component: CharacterDesc,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'CharacterDesc';
