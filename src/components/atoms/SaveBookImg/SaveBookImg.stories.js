import SaveBookImg from './SaveBookImg';

const metaConfig = {
  title: 'components/SaveBookImg',
  component: SaveBookImg,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'SaveBookImg';
