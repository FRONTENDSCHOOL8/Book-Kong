import Back from './Back';

const metaConfig = {
  title: 'components/Back',
  component: Back,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = '뒤로가기';
