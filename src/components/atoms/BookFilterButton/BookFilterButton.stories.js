import BookFilterButton from './BookFilterButton';

const metaConfig = {
  title: 'components/BookFilterButton',
  component: BookFilterButton,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookFilterButton';
