import TotalBookHeight from './TotalBookHeight';

const metaConfig = {
  title: 'components/TotalBookHeight',
  component: TotalBookHeight,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'TotalBookHeight';
