import ResetBtn from './ResetBtn';

const metaConfig = {
  title: 'components/ResetBtn',
  component: ResetBtn,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};

Base.storyName = 'ResetBtn';
