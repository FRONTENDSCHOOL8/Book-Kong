import ReviewInput from './ReviewInput';

const metaConfig = {
  title: 'components/ReviewInput',
  component: ReviewInput,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};

Base.storyName = 'ReviewInput';
