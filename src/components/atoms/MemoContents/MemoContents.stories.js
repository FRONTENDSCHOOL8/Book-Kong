import MemoContents from './MemoContents';

const metaConfig = {
  title: 'components/MemoContents',
  component: MemoContents,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'MemoContents';
