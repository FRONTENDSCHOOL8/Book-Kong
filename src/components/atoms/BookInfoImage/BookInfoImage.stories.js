import BookInfoImage from './BookInfoImage';

const metaConfig = {
  title: 'components/BookInfoImage',
  component: BookInfoImage,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookInfoImage';
