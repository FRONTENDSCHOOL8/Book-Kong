import BookRegistForm from './BookRegistForm';

const metaConfig = {
  title: 'components/BookRegistForm',
  component: BookRegistForm,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookRegistForm';
