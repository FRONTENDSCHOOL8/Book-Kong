import Header from './Header';

const metaConfig = {
  title: 'components/Header',
  component: Header,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'Header';
