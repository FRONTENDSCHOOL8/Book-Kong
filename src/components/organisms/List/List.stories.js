import List from './List';

const metaConfig = {
  title: 'components/List',
  component: List,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'List';
