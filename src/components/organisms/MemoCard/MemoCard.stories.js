import MemoCard from './MemoCard';

const metaConfig = {
  title: 'components/MemoCard',
  component: MemoCard,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'MemoCard';
