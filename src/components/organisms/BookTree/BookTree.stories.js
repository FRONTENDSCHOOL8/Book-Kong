import BookTree from './BookTree';

const metaConfig = {
  title: 'components/BookTree',
  component: BookTree,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookTree';
