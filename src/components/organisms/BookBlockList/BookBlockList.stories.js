import BookBlockList from './BookBlockList';

const metaConfig = {
  title: 'components/BookBlockList',
  component: BookBlockList,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookBlockList';
