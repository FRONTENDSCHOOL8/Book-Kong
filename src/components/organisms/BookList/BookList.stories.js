import BookList from './BookList.jsx';

const metaConfig = {
  title: 'components/List',
  component: BookList,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'BookList';
