import MemoRegistrationPage from './MemoRegistrationPage';

const metaConfig = {
  title: 'components/MemoRegistrationPage',
  component: MemoRegistrationPage,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'MemoRegistrationPage';
