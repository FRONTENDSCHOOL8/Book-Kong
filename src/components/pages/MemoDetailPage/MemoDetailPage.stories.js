import MemoDetailPage from './MemoDetailPage';

const metaConfig = {
  title: 'components/MemoDetailPage',
  component: MemoDetailPage,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = 'MemoDetailPage';
