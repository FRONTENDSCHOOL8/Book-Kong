import BookRegisterPage from './BookRegisterPage';

const metaConfig = {
  title: 'components/BookRegisterPage',
  component: BookRegisterPage,
  tags: ['autodocs'],
  args: {},
};

export default metaConfig;

export const Base = {
  args: {},
};
Base.storyName = '책 등록 페이지';
